/**
 * Review card -- Global component
 * Instructor overview -- Global Component
 * Courses Card 2 variation -- Global component
 * Course content -- Global / One for private With full access
 * Student also Bought -- Local card
 * 
 */

/**
 * need a function for filtering text
 * need a function for text slicing and showing 
 * ex: const slice =  (text,length)=> text.slice(0,length) + "...";
 */